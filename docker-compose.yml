services:
  db:
    image: postgres:17.5-alpine3.22
    container_name: postgres-db
    env_file:
      - .envrc
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  db-data:
